const create = require('./create');
const deleteBucket = require('./delete');
const exists = require('./exists');
const list = require('./list');

module.exports = {
  create,
  deleteBucket,
  exists,
  list,
};
